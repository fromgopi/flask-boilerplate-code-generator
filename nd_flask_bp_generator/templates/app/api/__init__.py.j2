from flask import Blueprint
from flask_restx import Api

api_bp = Blueprint('api', __name__, url_prefix='/api')
api = Api(
    api_bp,
    version='1.0',
    title='{{ project_name }} API',
    description='{{ description }}',
    doc='/docs'
)

from app.api import v1  # noqa 